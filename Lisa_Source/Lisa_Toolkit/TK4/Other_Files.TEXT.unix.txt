EXEC(sample)
SET sample TO UPPERCASE(sample)
IF sample='BOXER' THEN
    RETURN 'UPALETTE=.TEXT'
ENDIF
IF sample='CLOCK' THEN
    RETURN 'UTIMER=.TEXT'
ENDIF
IF sample='READER' THEN
    RETURN 'UICONREF=.TEXT'
ENDIF
RETURN ''
ENDEXEC
ÿ